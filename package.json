{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "yarn --cwd 'frontend' run dev",
    "build:frontend": " yarn wsrun --stages build"
  },
  "devDependencies": {
    "wsrun": "^5.2.0"
  }
}
